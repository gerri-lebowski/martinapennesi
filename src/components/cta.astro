---
const ctaLink = '/contatti';
---

<a href={ctaLink} class="cta">Prenota</a>

<style>
    .cta {
        background: var(--clr-accent);
        padding: .5em;
        border-radius: 4px;
        color: var(--clr-white);
        text-align: center;
        max-width: 10rem;
        margin-left: auto;
    }
</style>